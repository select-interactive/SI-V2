(function( doc, $ ) {

    window.SI = window.SI || {};

    var me, s, els;

    SI.module = {
    
        settings: {
        
        },

        elements: {
        
        },

        init: function() {
            me = this;
            s = me.settings;
            els = me.elements;
            me.bindEvents();
        },

        bindEvents: function() {
        
        }
    };
    
}( document, jQuery ) );